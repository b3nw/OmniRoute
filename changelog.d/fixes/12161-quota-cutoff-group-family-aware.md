- **fix(quota):** per-account quota cutoffs are now group/family-aware — a `qtSd/<group>/…` request is judged by the group's most depleted member connection (minimum remaining %, never a sum) and an Antigravity request is judged by its own family bucket (`gemini_weekly`/`claude_gpt_weekly`, `gemini_5h`/`claude_gpt_5h`), so a cutoff configured on a family bucket also governs that family's per-model windows and an exhausted Claude bucket no longer blocks a Gemini request — not through its own window, not through a group sibling's cached window, and not through the connection-wide `limitReached` summary. Both cutoff gates (direct chat preflight and the combo per-target check) share the same evaluator and classify by the actual resolved target model; non-group, non-family providers keep the raw per-window comparison ([#12161](https://github.com/diegosouzapw/OmniRoute/pull/12161))
