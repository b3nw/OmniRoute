- **feat(quota):** track the Umans AI prepaid wallet — balance/spend/request/concurrency telemetry on the Provider Limits page plus an absolute remaining-cash cutoff in cents (per connection or per provider) that stops selecting an account before it runs dry. Detected from the connection's base URL, so it works on the generic `openai-compatible-*` node Umans is configured as; fail-open throughout, so a wallet read that fails never blocks routing
